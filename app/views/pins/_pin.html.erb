<%= link_to pin do %>
  <div class="card">
    <%= image_tag pin.thumbnail.url, class: 'card-img-top' if pin.thumbnail.exists? %>
    <div class="card-header">
      <%= pin.name %>
    </div>
  </div>
<% end %>
